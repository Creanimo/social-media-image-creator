<wa-dialog id="crop-modal" label="Crop Image" style="--width: 80vw;">
    <div id="crop-wrapper" style="min-height: 400px; max-height: 60vh; position: relative;">
        <!-- Container for ImageCropper -->
        <div id="crop-container"></div>
    </div>
    <div slot="footer">
        <wa-button id="crop-cancel">Cancel</wa-button>
        {{#showOverride}}
        <wa-button id="crop-override">Override Original</wa-button>
        {{/showOverride}}
        <wa-button id="crop-save-new" variant="brand">Save as New</wa-button>
        <wa-button id="crop-no-crop" variant="brand">Don't crop</wa-button>
    </div>
</wa-dialog>
